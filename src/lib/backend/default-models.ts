/** Fallbacks for background tasks when the user hasn't picked a model in settings. */
export const DEFAULT_BACKGROUND_MODEL = 'deepseek/deepseek-v4-flash-0731'; // titles, follow-ups, memory
export const DEFAULT_UTILITY_MODEL = 'z-ai/glm-5.3-flash'; // prompt enhancer, image analysis (needs vision)
